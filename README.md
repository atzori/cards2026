# cards2026
The game with cards developed with Andrea Loi at UniCA

## Usage

	python -m venv venv 	# create a virtualenv
	. venv/bin/activate	# enter virtualenv
	pip install -r requirements.txt
	python app.py


Now go to http://localhost:8080/ and start playing (you must know the rules of the magic card game)
